# Claude Assistant Environment Configuration Template
# Copy this file to .env and fill in your actual API keys

# ===== Required API Keys =====

# Anthropic API key for Claude models
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Picovoice access key for wake word detection and STT
# Get yours at: https://console.picovoice.ai/
PORCUPINE_ACCESS_KEY=your-picovoice-key-here
PICOVOICE_ACCESS_KEY=your-picovoice-key-here

# ===== Optional API Keys =====

# OpenAI API key (only needed for legacy voice assistant)
# OPENAI_API_KEY=sk-proj-your-key-here

# ===== MCP Server Configuration =====

# Working directory for MCP server operations
MCP_WORKING_DIR=./data/sandbox

# Allowed tools for MCP server (space-separated)
MCP_ALLOWED_TOOLS=Bash Read Edit Write Glob Grep WebFetch WebSearch

# Permission mode for Claude Code operations
# Options: bypassPermissions, requireApproval
MCP_PERMISSION_MODE=bypassPermissions

# ===== Google Workspace MCP Configuration =====
# Get OAuth credentials at: https://console.cloud.google.com/

# Google OAuth Client ID (from Google Cloud Console)
GOOGLE_OAUTH_CLIENT_ID=your-client-id-here.apps.googleusercontent.com

# Google OAuth Client Secret (from Google Cloud Console)
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret-here

# Allow HTTP for OAuth (development only - remove for production)
OAUTHLIB_INSECURE_TRANSPORT=1

# Optional: Default Google account email for single-user mode
# USER_GOOGLE_EMAIL=your-email@gmail.com

# Optional: Google Programmable Search Engine (PSE) credentials
# GOOGLE_PSE_API_KEY=your-pse-api-key
# GOOGLE_PSE_ENGINE_ID=your-search-engine-id

# Optional: Enable OAuth 2.1 multi-user support
# MCP_ENABLE_OAUTH21=true

# Optional: Enable stateless mode (requires OAuth 2.1)
# WORKSPACE_MCP_STATELESS_MODE=true

# ===== ProtonMail MCP Configuration =====

# ProtonMail email address
PROTONMAIL_USERNAME=your-email@protonmail.com

# ProtonMail SMTP password (NOT your login password - get from ProtonMail settings)
PROTONMAIL_PASSWORD=your-smtp-password-here

# ProtonMail SMTP host (default: smtp.protonmail.ch)
PROTONMAIL_HOST=smtp.protonmail.ch

# ProtonMail SMTP port (default: 587 for STARTTLS, 465 for SSL/TLS)
PROTONMAIL_PORT=587

# Use secure connection (false for port 587, true for port 465)
PROTONMAIL_SECURE=false

# Optional: Enable debug logging
# DEBUG=true

# ===== Voice Assistant Configuration =====

# Working directory for voice assistant
VOICE_WORKING_DIR=./sandbox

# Audio feedback enabled
ENABLE_SOUND_EFFECTS=true

# TTS playback speed multiplier (e.g., 1.3 for 30% faster, 0.9 for 10% slower)
TTS_SPEED=1.3

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
